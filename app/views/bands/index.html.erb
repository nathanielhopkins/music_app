<h2 class="page-header">Bands</h2>

<ul>
  <% @bands.each do |band| %>
    <li class="band-index">
        <a href="<%= band_url(band) %>"><%= band.name%></a>
    </li>
  <% end %>
</ul>

<a class="button" href="<%= new_band_url %>">Add a Band</a>